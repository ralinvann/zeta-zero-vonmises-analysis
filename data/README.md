# Data Directory

This folder stores raw source files used to extract nontrivial zeta zeros.

Current `.dat` files are read by `src/extract_riemann_zeros.py`.

`zeros.txt` can be used as an optional human-readable export location if needed.
